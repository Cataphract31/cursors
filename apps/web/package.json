{
  "name": "@cursors/web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/smoke.mjs && vite build && node scripts/postbuild.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "webamp": "^1.5.0"
  },
  "devDependencies": {
    "vite": "^8.2.1",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
